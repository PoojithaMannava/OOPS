#__init__method whenever we create this init method it will call 
#as soon as class is created.
#if we want to store attributes initially for objects
#we declare them in class so that whenver the object is called/created from class.and 
class BankAccount:
    def __init__(self):
        print("this is automatically called")
ob1=BankAccount()

            
